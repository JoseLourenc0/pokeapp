import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pokeapp.joselourenco',
  appName: 'pokeapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
